# Environment Configuration Examples

# Caddy Proxy Configuration (for production deployment)
# MINICALEN_HOST=yourdomain.com           # The domain/hostname where Caddy listens
# MINICALEN_BACKEND=backend-service       # Backend service hostname/IP (Node.js server)
# MINICALEN_FRONTEND=frontend-service     # Frontend service hostname/IP (Vite or static files)

# Development (default values, no need to set these in development)
# VITE_API_URL=http://localhost:3001
# VITE_WS_URL=http://localhost:3001

# Production Examples
# For same-domain deployment with proxy
# VITE_API_URL=https://yourdomain.com
# VITE_WS_URL=wss://yourdomain.com

# For API on different domain/subdomain
# VITE_API_URL=https://api.yourdomain.com
# VITE_WS_URL=wss://api.yourdomain.com

# For API on different port
# VITE_API_URL=https://yourdomain.com:8080
# VITE_WS_URL=wss://yourdomain.com:8080

# Server Environment Variables
# NODE_ENV=production
# PORT=3001
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# MINICALEN_HOST=yourdomain.com  # Used for CORS when behind proxy

# HTTPS Configuration (optional)
# USE_HTTPS=true
# SSL_KEY_PATH=/path/to/private-key.pem
# SSL_CERT_PATH=/path/to/certificate.pem

# For development with custom hosts (e.g., Docker, remote development)
# VITE_API_URL=http://your-dev-server:3001
# VITE_WS_URL=http://your-dev-server:3001
# ALLOWED_ORIGINS=http://localhost:5173,http://your-dev-client:5173
